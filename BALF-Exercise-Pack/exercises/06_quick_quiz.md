# Exercise 06 – Quick Quiz

Choose the best answer:

1) A class handles: payment calculation, CSV export, and UI rendering. Which principle is mainly violated?
- A) DRY
- B) SRP
- C) YAGNI

2) You add Telegram notification support "just in case", but no requirement exists.
- A) DRY
- B) SRP
- C) YAGNI

3) Same validation logic appears in 4 services.
- A) DRY
- B) SRP
- C) YAGNI

4) @Spy vs @Mock (short):
- A) Spy uses real behavior by default, Mock does not
- B) Mock uses real behavior by default, Spy does not

5) In Mockito, which call checks interaction count?
- A) thenReturn(...)
- B) verify(..., times(n))
- C) doNothing()
