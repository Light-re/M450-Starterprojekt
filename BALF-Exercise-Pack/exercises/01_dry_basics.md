# Exercise 01 – DRY Basics

## Task A
You have this repetitive pseudo-code:

```text
if (a is even) { print("even") }
if (b is even) { print("even") }
if (c is even) { print("even") }
```

Refactor it so the check exists only once.

## Task B
Name 2 practical benefits of DRY in team projects.

## Task C
When can too much DRY become a problem (over-abstraction)?
Give one short example.
